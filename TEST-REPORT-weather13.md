# weather13.vue 테스트 보고서

- **대상 페이지**: `https://p4nteng.github.io/skala-vue/weathers/weather13` (배포) / `http://localhost:5173/skala-vue/weathers/weather13` (로컬)
- **대상 파일**: `src/pages/weathers/weather13.vue`
- **테스트 일시**: 2026-08-05 (로컬 1차) / 2026-08-05 (배포 재검증)
- **테스트 방법**: Playwright 브라우저 자동화 (데스크톱 1280×800 / 모바일 390×844)

---

## 1. 페이지 개요

OpenWeatherMap API를 사용하는 날씨 대시보드. 사이드바(shadcn-vue Sidebar)에 14개 기본 도시 + 사용자 추가 도시 목록, 검색·즐겨찾기·단위 선택(°C/°F)이 있고, 메인 영역에서 선택한 도시의 상세 날씨를 탭(Now / Hourly / 5-Day / Details)으로 제공한다.

## 2. 정상 동작 확인 ✅

| 항목 | 결과 |
|---|---|
| 페이지 로드 | 정상 (콘솔 에러 0건) |
| 14개 기본 도시 + 사용자 추가 도시 로드 | 정상 (지오코딩 + 현재 날씨 API, 좌표 캐시 적용) |
| 도시 선택 → 상세 뷰 | 정상 |
| Now 탭 | 온도·체감온도·설명·습도/바람/구름/기압/강수확률 표시 정상 |
| Hourly 탭 | 3시간 단위 8개 슬롯 표시 정상 |
| 5-Day 탭 | `cnt: 40` 기반 5일 예보 표시 정상 |
| Details 탭 | 일출/일몰/최고/최저 + 낮 진행도 + Heat Index + 대기질(AQI/PM) 표시 |
| 단위 토글(°F) | 전 탭 변환 정상 (사이드바·Now·Hourly·5-Day·Details), 바람도 m/s→mph 변환 |
| 검색 필터 | "서울" 입력 시 목록 필터링 정상 |
| 즐겨찾기 | 별 클릭 → 목록 맨 위 정렬 + `localStorage('weatherFavorites')` 영속화 정상 |
| 사이드바 접기/펼치기 | 정상 (256px ↔ 48px) |
| Weather 로고 버튼 | 클릭 시 선택 해제 → 즐겨찾기(Favorites) 뷰로 복귀 |
| "Updated" 타임스탬프 | 도시 선택 시 표시 정상 |
| 사용자 도시 추가/삭제 | 지오코딩 검색 드롭다운으로 추가, 리스트에서 삭제 정상 |

---

## 3. 버그 리포트 🐛 (수정 여부 포함)

> ✅ = 배포 페이지에서 수정 확인 완료

### 3-1. [심각] 전역 AppTopBar가 페이지 상단을 덮음 (데스크톱) — ✅ 수정됨

- **기존 증상**: 사이드바 토글 버튼(화면 좌표 약 280, 14)의 히트 테스트가 `AppTopBar`(고정 헤더, `z-50`, 높이 70px, 전폭)를 반환. **마우스로 클릭 불가**. 도시 이름·상태 배지도 가려짐
- **수정 내용**: `AppTopBar` 래퍼에 `pointer-events: none` 적용 (네비게이션 자체는 `pointer-events: auto`)
- **재검증 결과**: 사이드바 토글 클릭 성공, 사이드바 256px→48px 접힘/펼침 정상

### 3-2. [높음] Hourly / 5-Day 탭 온도 단위 미변환 — ✅ 수정됨

- **기존 증상**: °F로 전환해도 Hourly(36°…39°)와 5-Day(33°~39°)는 **섭씨 원본값 그대로 표시**. 단위 접미사도 없음
- **수정 내용**: Hourly/5-Day 템플릿에서 `convertTemp()` 적용 + `tempUnit()` 접미사 추가
- **재검증 결과**: °F 전환 시 Hourly **95°F~102°F**, 5-Day **84°F~102°F** 표시

### 3-3. [높음] 모바일 레이아웃 — 사이드바가 화면 66% 차지 — ✅ 수정됨

- **기존 증상**: 390px 뷰포트에서 사이드바 256px, 메인 콘텐츠 262px
- **수정 내용**: 모바일에서 사이드바가 아이콘(48px) 모드로 접힘
- **재검증 결과**: 390px에서 사이드바 **48px(12%)**, 메인 **342px(88%)** 확보

### 3-4. [낮음] "13 cities" 카피 오류 — ✅ 수정됨

- **기존 증상**: `DEFAULT_CITIES`는 14개인데 문구가 "13 cities"로 고정
- **수정 내용**: `{{ weatherList.length }} cities` 동적 표시
- **재검증 결과**: "14 cities · real-time OpenWeather data" 표시

### 3-5. [낮음] "5-Day" 탭이 6일 표시 — ✅ 수정됨

- **기존 증상**: 오늘 포함 6개 날짜(8.5~8.10) 표시
- **수정 내용**: `dailyForecast`가 오늘 포함 5개 날짜로 집계 (라벨과 일치)
- **재검증 결과**: 정확히 5일(8.5~8.9) 표시

### 3-6. [낮음] 날씨 코드 폴백 버그 — ✅ 수정됨

- **기존 증상**: 803(구름많음)/804(흐림)이 group 800 폴백으로 **맑음(Sun) 아이콘** 표시
- **수정 내용**: `803: Broken Clouds`, `804: Overcast` (Cloud 아이콘) 추가

---

## 4. 디자인 개선 제안 🎨 (수정 여부 포함)

### 4-1. 언어 통일 — ✅ 수정됨

- **기존**: 상태 배지 "Clear"(영문) ↔ 설명 "온흐림"(한글) 혼재
- **수정 내용**: 배지가 한글 `description`(API `lang=kr`) 표시
- **재검증 결과**: 헤더 배지에 "온흐림" 표시

### 4-2. 5-Day 예보 막대 스케일 — ✅ 수정됨

- **기존**: `(high - low) / high * 100%` — 고온일수록 짧고, 음수면 100% 초과
- **수정 내용**: `globalMin`/`globalRange`로 정규화 (`barLeft`/`barWidth` %)

### 4-3. 단위 설정 영속화 — ✅ 수정됨

- **기존**: 새로고침 시 °C로 리셋
- **수정 내용**: `localStorage('weather13Unit')` 저장/복원
- **재검증 결과**: 재로드 후 °F 복원 실측 확인 (서울 95°F)

### 4-4. Clear 상태 색상 — ✅ 수정됨

- **기존**: `#141413`(검정)이 다크모드에서 거의 보이지 않음
- **수정 내용**: 다크모드에서 `#E5E5E0`로 전환 (`getCondition`에서 처리)

### 4-5. API 호출 절감 — ✅ 수정됨

- **기존**: 마운트마다 지오코딩 14회 + 현재 날씨 14회 = 28회 호출
- **수정 내용**: `CITY_COORDS` 좌표 캐시 — `fetchCoords()`가 캐시 히트 시 API 호출 생략

### 4-6. 빈 상태 문구 동적 표시 — ✅ 수정됨

- `{{ weatherList.length }}` 동적 카운트 적용 (3-4와 동일)

### 4-7. 사이드바 "Weather" 로고 버튼 — ✅ 수정됨 (재검증으로 정정)

- **기존**: 클릭 액션 없음으로 판정 (1차 검증 오류)
- **수정 내용**: `@click="resetSelection"` — 도시 상세에서 클릭 시 **즐겨찾기(Favorites) 뷰로 복귀** (즐겨찾기 없으면 빈 상태)
- **재검증 결과**: 부산 즐겨찾기 추가 → 서울 상세 → Weather 클릭 → "Favorites / 부산 88°F" 그리드 표시 확인

---

## 5. 배포(GitHub Pages) 검증 노트

### 5-1. 리포트 항목 최종 상태 요약

| 구분 | 항목 | 상태 |
|---|---|---|
| 버그 | 3-1 AppTopBar 겹침 / 3-2 단위 미변환 / 3-3 모바일 사이드바 / 3-4 카피 / 3-5 5-Day 6일 / 3-6 아이콘 폴백 | **전부 수정 ✅** |
| 제안 | 4-1 ~ 4-7 (언어·막대·영속화·색상·API·문구·로고버튼) | **전부 수정 ✅** |

### 5-2. 정적 호스팅 제약 (알려진 이슈, 리포트 항목 아님)

- **딥링크 직접 접속/새로고침 시 404**: `https://p4nteng.github.io/skala-vue/weathers/weather13`을 주소창에 직접 입력하거나 해당 URL에서 새로고침하면 GitHub Pages가 404를 반환 (SPA 폴백 없음, `createWebHistory` 한계)
- **앱 내부 링크 이동(히스토리 pushState)은 정상**: 홈 → Weathers → weather13 링크 클릭 이동은 문제없음
- **해결 방안**: `public/404.html`에 `index.html`을 복사하는 스니펫(또는 `vite-plugin` 기반 404 폴백)을 추가하면 딥링크가 정상 동작

### 5-3. 배포 검증 시 확인한 신규 기능 (리포트 작성 이후 추가)

- **Heat Index / Wind Chill**: Details 탭에 표시 (예: Heat Index 104°F)
- **대기질(AQI)**: Details 탭에 등급 + PM2.5/PM10 (예: Poor, 56.59 μg/m³)
- **사용자 정의 도시**: 지오코딩 검색 드롭다운으로 도시 추가, 리스트에서 삭제 (`weather13UserCities` 영속화)
- **낮 진행도 바**: 일출~일몰 진행률 + 남은 시간("2h 16m left")
- **Updated 타임스탬프**: 헤더에 마지막 갱신 시각

---

## 6. 우선순위 요약 (원본 기준 → 현재)

| 순위 | 항목 | 영향 | 상태 |
|---|---|---|---|
| 1 | AppTopBar 헤더 겹침 (토글 버튼 클릭 불가) | 데스크톱 핵심 기능 차단 | ✅ 수정 |
| 2 | Hourly/5-Day 단위 미변환 | 데이터 정확성 | ✅ 수정 |
| 3 | 모바일 사이드바 66% 점유 | 모바일 사용성 | ✅ 수정 |
| 4 | 803/804 아이콘 폴백 | 데이터 표시 오류 | ✅ 수정 |
| 5 | 카피/라벨/단위 영속화 등 | 마이너 개선 | ✅ 수정 |

**남은 이슈**: 정적 호스팅 딥링크 404 (선택적 개선 — `404.html` 폴백 적용 가능)

---

*테스트 환경: GitHub Pages 배포판 + Vite dev server, Chrome(Playwright), OpenWeatherMap API 실데이터 기준*
